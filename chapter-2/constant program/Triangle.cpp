#include <iostream>
using namespace std;

int main()
{
    // write a program to find the area of a triangle
    // formala : 1/2(b * h)
    float b, h;
    cout << "Enter the value of base and hieght :";
    cin >> b >> h;
    cout << "Area of the triangle is :" << 0.5 * b * h << endl;
}